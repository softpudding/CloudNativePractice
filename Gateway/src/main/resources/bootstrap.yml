server:
  port: 8080
spring:
  application:
    name: bootstrap
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8888/
      username: user
      password: 666666
      discovery:
        service-id: configserver

